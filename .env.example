# Foundation startup only. No model credentials are required or consumed.
HOST=127.0.0.1
PORT=4177
VAR_DIR=./var
EXECUTION_MODE=auto
RUN_DISPATCH_MODE=inline
# All execution modes remain refused until a runner is installed in Phase 2.
