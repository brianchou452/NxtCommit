interface:
  display_name: "YAML 規格產碼"
  short_description: "從 YAML source spec 產生可驗證程式碼"
  default_prompt: "使用 $yaml-spec-to-code。先表明現在是電腦 A、B 或 C，以及 Phase 0–4；再從 YAML source spec 重建程式碼、宣告的 BDD 測試與 visual 驗證。即使 src、server 或 shared 尚不存在也直接產生。"
