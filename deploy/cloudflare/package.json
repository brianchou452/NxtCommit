{
  "name": "nxtcommit-delivery",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test worker.test.mjs",
    "build": "wrangler deploy --dry-run --outdir ../../artifacts/worker",
    "deploy": "wrangler deploy"
  },
  "devDependencies": { "wrangler": "4.131.1" },
  "engines": { "node": ">=22.0.0 <23" }
}
