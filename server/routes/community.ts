import { Router } from 'express';
import type { RouteModule } from './types.js';

/** Phase 2 Computer A module; no endpoints or simulated success yet. */
export const communityRoutes: RouteModule = _context => Router();
