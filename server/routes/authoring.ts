import { Router } from 'express';
import type { RouteModule } from './types.js';

/** Phase 2 Computer C module; no endpoints or simulated success yet. */
export const authoringRoutes: RouteModule = _context => Router();
